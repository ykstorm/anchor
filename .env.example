# Required
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
OPENAI_API_KEY=sk-...

# Optional (for Neon serverless — use Direct URL for migrations)
# DIRECT_URL=postgresql://user:pass@host/db?sslmode=require&connection_limit=1