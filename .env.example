# OpenAI — required for embedding
OPENAI_API_KEY=sk-...

# Postgres — for local dev, docker-compose handles this:
DATABASE_URL="postgresql://anchor:anchor_dev@localhost:5432/anchor?sslmode=disable"
DIRECT_URL="postgresql://anchor:anchor_dev@localhost:5432/anchor?sslmode=disable"

# Demo mode (shows refused: true responses in UI)
NEXT_PUBLIC_DEMO_MODE=true
